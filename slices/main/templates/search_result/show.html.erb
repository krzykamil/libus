

<% if book %>
  <div class="card lg:card-side bg-base-100 shadow-xl">
    <div class="card-body">
      <h2 class="card-title"><%= book.title %></h2>
      <p><%= book.description[0..500] %></p>
      <div class="card-actions justify-end">
        <!-- TODO: -->
        <button class="btn btn-primary">Add to your collection</button>
      </div>
    </div>
  </div>
<% else %>
  <div class="card lg:card-side bg-base-100 shadow-xl">
    <div class="card-body">
      <h2 class="card-title">No book found</h2>
      <p>Sorry, we couldn't find a book with that ISBN. Please try again.</p>
    </div>
  </div>
<% end %>
